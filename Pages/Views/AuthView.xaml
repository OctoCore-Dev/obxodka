<?xml version="1.0" encoding="utf-8" ?>
<ContentView
    x:Class="obxodka.Views.AuthView"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:mi="http://www.aathifmahir.com/dotnet/2022/maui/icons">
    <Grid>
        <VerticalStackLayout
            x:Name="ContentLayout"
            Padding="{OnIdiom Phone='24,48',
                              Desktop='0,40'}"
            HorizontalOptions="Center"
            Spacing="0"
            VerticalOptions="Center">

            <VerticalStackLayout
                x:Name="AppIconLeft"
                Margin="0,0,0,28"
                HorizontalOptions="Center"
                Spacing="10">
                <Image
                    HeightRequest="90"
                    HorizontalOptions="Center"
                    Source="app_logo.png"
                    VerticalOptions="Center"
                    WidthRequest="90" />
                <Label
                    x:Name="TitleLabelLogin"
                    CharacterSpacing="2.5"
                    HorizontalOptions="Center"
                    Style="{StaticResource TitleLarge}"
                    Text="ОБХОДКА VPN" />
            </VerticalStackLayout>

            <Border
                x:Name="FormContainer"
                Padding="28,24"
                HorizontalOptions="Center"
                Style="{StaticResource NeoPanel}"
                WidthRequest="{OnIdiom Phone=340,
                                       Default=400}">

                <VerticalStackLayout Spacing="0">

                    <Label
                        x:Name="AuthErrorLabel"
                        Margin="0,0,0,12"
                        AutomationId="AuthErrorLabel"
                        IsVisible="False"
                        Opacity="0"
                        Style="{StaticResource LabelError}"
                        TranslationY="-8" />

                    <Grid>
                        <VerticalStackLayout x:Name="EmailInputView" Spacing="14">
                            <Label
                                Margin="0,0,0,10"
                                HorizontalOptions="Center"
                                HorizontalTextAlignment="Center"
                                Style="{StaticResource LabelMuted}"
                                Text="Быстрый вход и регистрация без паролей. Один клик — и вы в безопасности." />

                            <Border Style="{StaticResource NeoInput}">
                                <Entry
                                    x:Name="EmailEntry"
                                    AutomationId="AuthEmailInput"
                                    Keyboard="Email"
                                    Placeholder="Ваш Email" />
                            </Border>

                            <Border
                                Margin="0,4,0,0"
                                HeightRequest="48"
                                Style="{StaticResource NeoCard}">
                                <Grid>
                                    <Button
                                        x:Name="GetCodeButton"
                                        AutomationId="AuthGetCodeButton"
                                        BackgroundColor="Transparent"
                                        Clicked="OnGetCodeClickedAsync"
                                        FontFamily="RobotoMedium"
                                        FontSize="14"
                                        Text="Получить код"
                                        TextColor="{AppThemeBinding Light=#333333,
                                                                    Dark=#E5E5E5}" />
                                    <ActivityIndicator
                                        x:Name="GetCodeLoader"
                                        HeightRequest="24"
                                        HorizontalOptions="Center"
                                        IsRunning="True"
                                        IsVisible="False"
                                        VerticalOptions="Center"
                                        WidthRequest="24"
                                        Color="{AppThemeBinding Light=#333333,
                                                                Dark=#E5E5E5}" />
                                </Grid>
                            </Border>
                        </VerticalStackLayout>

                        <VerticalStackLayout
                            x:Name="CodeInputView"
                            IsVisible="False"
                            Spacing="14">
                            <Label
                                Margin="0,0,0,10"
                                HorizontalOptions="Center"
                                HorizontalTextAlignment="Center"
                                Style="{StaticResource LabelMuted}"
                                Text="Код отправлен на почту. Введите его ниже." />

                            <Border Style="{StaticResource NeoInput}">
                                <Entry
                                    x:Name="CodeEntry"
                                    AutomationId="AuthCodeInput"
                                    Keyboard="Numeric"
                                    Placeholder="6-значный код" />
                            </Border>

                            <Label
                                Margin="0,-4,0,0"
                                FontSize="10"
                                HorizontalOptions="Center"
                                HorizontalTextAlignment="Center"
                                Style="{StaticResource LabelMuted}"
                                Text="Код отправлен на вашу почту, проверьте папку «Спам».&#x0a;Официальный аккаунт: noreply.obxodkavpn@gmail.com" />

                            <Border
                                Margin="0,4,0,0"
                                HeightRequest="48"
                                Style="{StaticResource NeoCard}">
                                <Grid>
                                    <Button
                                        x:Name="VerifyCodeButton"
                                        AutomationId="AuthVerifyCodeButton"
                                        BackgroundColor="Transparent"
                                        Clicked="OnVerifyCodeClickedAsync"
                                        FontFamily="RobotoMedium"
                                        FontSize="14"
                                        Text="Войти"
                                        TextColor="{AppThemeBinding Light=#333333,
                                                                    Dark=#E5E5E5}" />
                                    <ActivityIndicator
                                        x:Name="VerifyCodeLoader"
                                        HeightRequest="24"
                                        HorizontalOptions="Center"
                                        IsRunning="True"
                                        IsVisible="False"
                                        VerticalOptions="Center"
                                        WidthRequest="24"
                                        Color="{AppThemeBinding Light=#333333,
                                                                Dark=#E5E5E5}" />
                                </Grid>
                            </Border>

                            <Button
                                BackgroundColor="Transparent"
                                Clicked="OnBackToEmailClickedAsync"
                                FontFamily="RobotoMedium"
                                FontSize="12"
                                Text="Изменить Email"
                                TextColor="{AppThemeBinding Light=#666666,
                                                            Dark=#AAAAAA}" />
                        </VerticalStackLayout>
                    </Grid>
                </VerticalStackLayout>
            </Border>
        </VerticalStackLayout>
    </Grid>
</ContentView>
