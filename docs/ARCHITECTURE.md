# 📐 Архитектура и спецификация экосистемы Obxodka

Данный документ содержит полное, объективное и детальное техническое описание архитектуры клиентской и серверной частей **Obxodka** — высокоскоростного стелс-VPN нового поколения, разработанного для обхода систем глубокого анализа пакетов (**DPI / ТСПУ**) с нулевыми потерями скорости и строгой политикой **Zero-Log**.

---

## 🗺️ Высокоуровневая архитектурная схема

```
+========================================================================================+
|                              КЛИЕНТСКИЙ УРОВЕНЬ (CLIENT)                               |
|                                                                                        |
|   +--------------------------------------------------------------------------------+   |
|   |                        .NET 10 MAUI UI & State Manager                         |   |
|   |         (Event-Driven View Architecture, SkiaSharp 60 FPS Engine, DI)          |   |
|   +---------------------------------------+----------------------------------------+   |
|                                           |                                            |
|                  +------------------------+------------------------+                  |
|                  v                                                 v                   |
|   +------------------------------+                  +------------------------------+   |
|   |   Windows: Wintun Driver     |                  |   Android: VpnService        |   |
|   |  (64 MB Ring Buffer, P/Invoke|                  | (Foreground Service, FGS,    |   |
|   |  Subnet Routes 0/1 + 128/1)  |                  |  WakeLock, Network Roaming)  |   |
|   +--------------+---------------+                  +--------------+---------------+   |
|                  |                                                 |                   |
|                  +------------------------+------------------------+                   |
|                                           v                                            |
|   +--------------------------------------------------------------------------------+   |
|   |                      OctopusEngine (Core Network Gateway)                      |   |
|   |   - PacketRouter: Multi-Ray Классификатор (Realtime vs Bulk Traffic)           |   |
|   |   - PacketDeduplicator: Lockless кольцевой буфер (8192 слота, 5-Tuple)         |   |
|   |   - DpiBypassStream: TCP Initial Segment Splitting (2 байта + 25ms Delay в SocketsHttpHandler.ConnectCallback) |   |
|   |   - Obfuscator: Динамический шум и нормализация энтропии Шеннона (> 7.0)       |   |
|   |   - DnsAdBlocker: Встроенный L3 DNS Sinkhole для фильтрации рекламы            |   |
|   +-------------------+------------------------------------+-----------------------+   |
|                       |                                    |                           |
|                       v (Режим gRPC)                       v (Режим FECHSUE)           |
|   +-------------------------------------+   +--------------------------------------+   |
|   |        GrpcTransport (HTTP/2)       |   |      FechsueTransport (UDP 443)      |   |
|   |  - Двунаправленный gRPC-стрим       |   |  - Шифрование AES-256-GCM            |   |
|   |  - Multi-Ray (до 8 параллельных дуг)|   |  - Атомарный 96-bit Nonce            |   |
|   |  - mTLS + SPKI SHA-256 Pinning      |   |  - FEC 4:1 (Упреждающая коррекция)   |   |
|   +-------------------+-----------------+   +------------------+-------------------+   |
+=======================|========================================|=======================+
                        |                                        |
                        | (Порт :443 TLS 1.3 / gRPC)             | (Порт :443 UDP AES-GCM)
                        v                                        v
+========================================================================================+
|                            СЕТЕВАЯ ИНСПЕКЦИЯ (DPI / ТСПУ)                              |
|                                                                                        |
|   * Сегментация заголовка ClientHello на уровне L4/TCP для обхода сигнатурных фильтров.|
|   * Отсутствие стандартных протокольных маркеров WireGuard (0x01) и OpenVPN (0x38).    |
|   * Управление SNI и строгая валидация цепочки сертификатов X.509.                     |
|   * QUIC-inspired camouflage framing для первичных пакетов аутентификации FECHSUE.     |
+========================================================================================+
                        |                                        |
                        v                                        v
+========================================================================================+
|                              СЕРВЕРНЫЙ КЛАСТЕР (SERVER)                                |
|                                                                                        |
|   +--------------------------------------------------------------------------------+   |
|   |                          Caddy v2 / Kestrel mTLS Gateway                       |   |
|   |         (TLS 1.3 Termination, Camouflage Reverse Proxy на Google CDN)          |   |
|   +-------------------+------------------------------------+-----------------------+   |
|                       |                                    |                           |
|                       v                                    v                           |
|   +-------------------------------------+   +--------------------------------------+   |
|   |   obxodka.Octopus (gRPC Daemon)     |   |  obxodka.Octopus (Fechsue Daemon)    |   |
|   |  - TunnelGrpcService (Multiplexing) |   |  - FechsueServerService (AES-GCM)    |   |
|   |  - SessionManager & Auth Validation |   |  - Высокоскоростной пул воркеров     |   |
|   +-------------------+-----------------+   +------------------+-------------------+   |
|                       |                                        |                       |
|                       +-------------------+--------------------+                       |
|                                           v                                            |
|   +--------------------------------------------------------------------------------+   |
|   |                    LinuxTun (libc P/Invoke Zero-Copy I/O)                      |   |
|   |  - Прямой ввод-вывод: read(fd) / write(fd) через небезопасные указатели        |   |
|   |  - Виртуальный интерфейс tun0 (MTU 1280, txqueuelen 10000)                    |   |
|   |  - Ядро Linux: TCP BBR Congestion Control, TCPMSS Clamping                     |   |
|   |  - Маршрутизация и NAT (iptables MASQUERADE для 100.64.0.0/10 и fd00::/64)     |   |
|   +---------------------------------------+----------------------------------------+   |
|                                           v                                            |
|   +--------------------------------------------------------------------------------+   |
|   |                          Бэкенд-сервисы и База Данных                          |   |
|   |  - obxodka.Api: REST API (Source-Generated JSON, Anti-DDoS, Billing)           |   |
|   |  - obxodka.Security: RootCaService (Генерация 4096-bit RSA CA + NIST P-256)   |   |
|   |  - PostgreSQL 14: Пользователи, Устройства, Логи трафика, Отзывы               |   |
|   |  - Фоновые воркеры: GooglePlaySyncWorker, MicrosoftStoreSyncWorker, Hydra      |   |
|   +--------------------------------------------------------------------------------+   |
+========================================================================================+
```

---

## 1. Клиентская подсистема (Client Architecture)

### 1.1. Платформенный уровень Windows (Wintun Layer 3 Driver)
* **Интерфейс драйвера (`Platforms/Windows/WintunAdapter.cs`):**
  * Прямой P/Invoke к нативной библиотеке `wintun.dll` через `[LibraryImport]` с разрешением путей через `NativeLibrary.SetDllImportResolver`.
  * Создание адаптера TUN с фиксированным GUID сессии и кольцевым буфером объёмом **64 МБ** (`0x4000000`).
  * **TX (Upload):** Выделенный поток с наивысшим приоритетом `ThreadPriority.Highest` (`"Wintun-UploadReader"`). Пакеты вычитываются пачками до 256 штук за итерацию с ожиданием Win32-события через `WaitForSingleObject` (таймаут 200 мс).
  * **RX (Download):** Выделенный поток `ThreadPriority.Highest` (`"Wintun-Downloader"`), отправляющий пакеты в драйвер через `WintunAllocateSendPacket` и `WintunSendPacket`.
* **Безопасная маршрутизация (`Platforms/Windows/WindowsVpnService.cs`):**
  * Для предотвращения разрушения системных маршрутов трафик перехватывается двумя перекрывающимися маршрутами подсетей `/1`:
    ```cmd
    route add 0.0.0.0 mask 128.0.0.0 <AssignedIp> metric 1 if <ifIndex>
    route add 128.0.0.0 mask 128.0.0.0 <AssignedIp> metric 1 if <ifIndex>
    ```
  * Добавляется прямой маршрут к шлюзу через физический интерфейс (`Bypass Route`).
  * **Предотвращение утечек DNS (DNS Leak Protection):**
    * Установка явных статических хостовых маршрутов `/32` (`255.255.255.255`) ко всем серверам из пула `NetworkDefaults.TrustedDnsServers` (`1.1.1.1`, `1.0.0.1`, `8.8.8.8`, `8.8.4.4`, `9.9.9.9`, `149.112.112.112`, `77.88.8.8`) через виртуальный интерфейс Wintun (`route add <dnsIp> mask 255.255.255.255 <AssignedIp> metric 1 if <ifIndex>`), что гарантирует прохождение всех DNS-запросов строго через шифрованный туннель.
    * Защита от утечек многоадресного разрешения имён (Smart Multi-Homed Name Resolution): установка ключа реестра `DisableSmartNameResolution = 1` по пути `HKLM\SOFTWARE\Policies\Microsoft\Windows NT\DNSClient` (предотвращает параллельную отправку запросов на DNS физических сетевых интерфейсов Windows).
    * Принудительный сброс системного кэша резолвера `ipconfig /flushdns` при установке и корректном разрыве сессии.
  * **Жизненный цикл процесса и скрытие UI:** При нажатии пользователем кнопки закрытия окна приложение мгновенно скрывает UI (`ShowWindow SW_HIDE` / `AppWindow.Hide`), а остановка ядра VPN, удаление маршрутов, восстановление реестра и сброс DNS выполняются в фоне (до 5 секунд) без зависания интерфейса и без навязчивого пребывания в системном трее.
  * **Экстремальный разгон TCP (`ApplyExtremeNetworkBoostAsync`):**
    Включение `netsh int tcp set global autotuninglevel=experimental`, `congestionprovider=ctcp`, `ecncapability=enabled`, `fastopen=enabled`.

### 1.2. Платформенный уровень Android (Android VpnService)
* **Класс службы (`Platforms/Android/OctopusVpnService.cs`):**
  * Наследует `Android.Net.VpnService`, зарегистрирован с типом Foreground Service `specialUse` (поддержка Android 14+ / API 34+).
  * **Конфигурация TUN:** Универсальный MTU 1280 (`NetworkDefaults.DefaultMtu`), маршруты `0.0.0.0/0` и `::/0`, централизованный доверенный Anycast-пул DNS (`NetworkDefaults.TrustedDnsServers`).
  * **Централизованные сетевые параметры (`NetworkDefaults.cs`):** Кроссплатформенный модуль конфигурации в `obxodka.Shared`, синхронизирующий универсальный MTU 1280, безопасное сжатие (`ClampMtu(1280, 1420)`), основной и резервный DNS (`1.1.1.1` / `1.0.0.1`), а также умный алгоритм динамической оценки доступности серверов DNS (`GetHealthyDnsServersAsync`) через параллельные UDP-пробы DNS.
  * **Split Tunneling:** Метод `builder.AddDisallowedApplication(pkg)` позволяет исключать выбранные приложения из VPN.
  * **Защита сокетов:** Защита дескрипторов сокетов через `Protect((int)sock.Handle)` (включая диагностические сокеты InSitu через `HookProtection`) предотвращает зацикливание сетевого ввода-вывода.
  * **Энергосбережение и роуминг:** Удержание `WakeLockFlags.Partial` и автоматический реконнект при переключении сетей Wi-Fi ↔ LTE через `ConnectivityManager.NetworkCallback`.

---

## 2. Сетевые протоколы и Stealth-транспорты

### 2.1. GrpcTransport (HTTP/2 & HTTP/3 Multi-Ray)
* **Стриминг:** Двунаправленный gRPC-стрим `TunnelService.ConnectStream` поверх оптимизированного `SocketsHttpHandler`.
* **Multi-Ray Routing (`PacketRouter.cs`):**
  * Разделение трафика на 8 параллельных каналов (лучей).
  * **Realtime / Gaming:** Чувствительный к задержкам трафик (ICMP, DNS, малые UDP-пакеты до 600 байт, TCP SYN/ACK) направляется в **Ray 0** и дублируется в **Ray 7** (крайний луч для максимальной надёжности и компенсации джиттера).
  * **Bulk:** Тяжёлый TCP-трафик хэшируется по 4-tuple (`srcIp:srcPort:dstIp:dstPort`) и распределяется по 6 параллельным лучам **Ray 1..6** (`1 + (absHash % 6)`).
* **Дедупликация (`PacketDeduplicator.cs`):** Атомарный циклический буфер на 8192 слота отсекает дубликаты на стороне приёма без блокировок потоков.

### 2.2. FechsueTransport (Stealth UDP + AES-256-GCM + FEC)
* **Спецификация кадра:**
  ```
  +------------------+-------------------+---------------------------+-------------------+
  | Nonce (12 байт)  | Header (4 байта)  | Зашифрованный IP Payload  | GCM Tag (16 байт) |
  | (Atomic Counter) | (Session Mask ID) | (AES-256-GCM Ciphertext)  | (Poly1305 / GCM)  |
  +------------------+-------------------+---------------------------+-------------------+
  ```
* **Прямая коррекция ошибок (`FecEngine.cs`):**
  * Схема 4:1 — на каждые 4 пакета данных вычисляется 1 дополнительный XOR-паритетный шард.
  * Позволяет восстановить 1 потерянный пакет из 4 с нулевой задержкой (0 RTT loss recovery).

### 2.3. Механизмы обхода DPI и ТСПУ
1. **TCP Payload Splitting (`DpiBypassStream.cs` & `ChameleonState.cs`):**
   * Первые 1–2 байта записи TLS ClientHello (`ChameleonState.GetNextSplitSize()`) отправляются отдельным TCP-сегментом, после чего следует адаптивная пауза 15–25 мс (`ChameleonState.GetNextDelayMs()`) перед отправкой тела ClientHello с SNI.
   * Системы DPI, анализирующие первый сегмент соединения, не могут обнаружить SNI и пропускают поток.
2. **Dynamic Noise Padding (`Obfuscator.cs`):**
   * Добавление случайного мусорного хвоста (от 1 до 128 байт) к каждому пакету разрушает статистические профили распределения длин пакетов.
   * Энтропия Шеннона поддерживается на уровне > 7.0 (аналогично чистому TLS 1.3).
3. **Domain Fronting & SNI Spoofing:**
   * В TLS ClientHello поле SNI устанавливается в доверенные глобальные домены (`google.com`, `cloudflare.com`), в то время как физический сокет направляется на боевую ноду Obxodka.

---

## 3. Серверная инфраструктура (Server Architecture)

### 3.1. Ядро сетевой обработки `obxodka.Octopus`
* **Прямой доступ к Linux TUN (`LinuxTun.cs`):**
  * Инициализация `/dev/net/tun` через `ioctl(fd, TUNSETIFF)`.
  * Прямой ввод-вывод на небезопасных указателях `read(fd, byte*, count)` и `write(fd, byte*, count)` в обход абстракций рантайма .NET.
  * Виртуальный интерфейс `tun0` с MTU 1280 (`NetworkDefaults.DefaultMtu`), txqueuelen 10000.
  * Автоматическая настройка ядра Linux: `sysctl net.ipv4.ip_forward=1`, `iptables MASQUERADE` для подсети `100.64.0.0/10` и `fd00::/64`, `TCPMSS --set-mss 1240` (DefaultMtu - 40) и `--clamp-mss-to-pmtu`.
* **Фоновые службы и воркеры:**
  * **`TunReaderService`:** Выделенный нативный поток ОС с приоритетом `ThreadPriority.Highest`, непрерывно вычитывающий пакеты из ядра Linux и маршрутизирующий их по сессиям клиентов.
  * **`SyncWorker`:** 10-секундный цикл сбора статистики трафика, сверки с биллингом API и немедленного сброса сессий из чёрного списка (CRL).
  * **`HydraAutomationService`:** Автоматический генератор Cloudflare Worker мостов с ротацией каждые 24 часа.

### 3.2. REST API и безопасность `obxodka.Api`
* **Anti-DDoS Middleware (`AntiDdosMiddleware.cs`):**
  * Извлечение реального IP через `CF-Connecting-IP` / `X-Forwarded-For`.
  * При накоплении 20 ошибок (401, 403, 429) IP-адрес блокируется на 15 минут с возвратом кода 429 и заголовка `Retry-After`.
* **Root CA и mTLS генерация (`RootCaService.cs`):**
  * Самоподписанный 4096-bit RSA Root CA со сроком действия 10 лет.
  * Генерация пользовательских сертификатов ECDSA NIST P-256 на лету при авторизации.
  * Экспорт в зашифрованные контейнеры PKCS#12 (.pfx).
* **Синхронизация отзывов магазинов:**
  * `GooglePlaySyncWorker`: ежечасный опрос Google Play Developer API v3.
  * `MicrosoftStoreSyncWorker`: ежечасный опрос Microsoft Dev Center Analytics API (Azure AD OAuth2).

---

## 4. Спецификация базы данных (PostgreSQL)

Схема данных включает 9 ключевых таблиц:
* **`Users`**: Идентификатор, Email, хэш пароля, отпечаток сертификата mTLS, баланс секунд, время подписки, статистика трафика, реферальный код, массив достижений.
* **`Devices`**: Привязанные устройства (до 4 шт. на аккаунт), HWID, имя, зашифрованный сертификат, дата последней активности.
* **`Orders`**: Заказы, платежи AnyPay/Google Play, валюта, статус, начисленные часы.
* **`TrafficLogs`**: Посессионные дельты трафика по протоколам.
* **`Reviews` & `ReviewLikes`**: Отзывы со всех платформ (Web, Google Play, Microsoft Store), рейтинги, версии приложений, древовидные ответы.
* **`ReferralActivations`**: Реферальная статистика, начисленные бонусные часы.
* **`AuthCodes`**: Одноразовые коды авторизации (Email OTP) со сроком жизни 10 минут.
* **`BugReports`**: Диагностические отчёты пользователей, логи исключений, сведения об ОС и версии клиента для оперативного выявления инцидентов.

---

## 5. Политика Zero-Log и изоляция данных

```
+------------------------------------+------------------------------------+
| ДАННЫЕ В СЕРВЕРНОЙ БД (POSTGRESQL) |    ДАННЫЕ В RAM (ZERO-LOG ПОЛИТИКА)|
+------------------------------------+------------------------------------+
|  Email и хэш пароля (PBKDF2)       |  IP-адреса посещаемых сайтов (НЕТ) |
|  Баланс времени подписки           |  DNS-запросы и домены (НЕТ)        |
|  Общий объем использованных байт   |  Тела сетевых пакетов (НЕТ)        |
|  Отпечаток mTLS сертификата        |  История интернет-активности (НЕТ) |
+------------------------------------+------------------------------------+
```

1. **Эфемерная память:** Все сетевые буферы арендуются через `ArrayPool<byte>.Shared` для предотвращения давления на сборщик мусора (GC) и исключения аллокаций в Gen0/Gen1 на горячем пути передачи. Память возвращается в пул сразу после отправки пакета в сокет (без гарантии криптографического зануления несекретных транзитных данных).
2. **Отсутствие дискового логирования:** Серверы Obxodka не ведут журналов сетевой активности (PCAP/Flow logs), исключая возможность восстановления истории сёрфинга даже при физическом изъятии оборудования.

---

## 6. Перспективная эволюция: Полиморфный транспорт без сигнатур (Future Roadmap)

В ответ на развитие систем машинного обучения ТСПУ/DPI и возможный переход сетевых регуляторов к режиму жестких **«Белых списков» (Whitelisting)**, проектируется архитектурный переход от статичной маскировки (под HTTP/TLS/VoIP-звонки) к динамической полиморфной системе передачи данных.

### 6.1. Ограничения классических подходов
1. **Смерть высокой энтропии:** Чистый шифрованный поток (энтропия Шеннона $\approx 8.0$) мгновенно классифицируется DPI как аномалия и блокируется или замедляется.
2. **Уязвимость статических шаблонов:** Любая имитация известных протоколов (звонки WebRTC, HTTP-запросы) имеет характерные тайминги, размеры первых кадров и реакции на сокеты. Нейросети выявляют эти отклонения за считанные дни.
3. **Активное зондирование (Active Probing):** Прямая проверка порта сервера цензором приводит к компрометации ноды, если она не является реальным веб-сервером.
4. **Белые списки:** Полная блокировка неизвестных внешних IP-адресов делает бессмысленным прямое соединение с зарубежными серверами.

### 6.2. Три технологических столпа нового поколения

```
+-----------------------------------------------------------------------------------------------+
|                      АРХИТЕКТУРА УСТОЙЧИВОГО ПОЛИМОРФНОГО ТРАНСПОРТА                          |
+-------------------------------+-------------------------------+-------------------------------+
|  1. ШЕЙПИНГ РАСПРЕДЕЛЕНИЯ     |  2. РАСПЫЛЕНИЕ (SCATTERING)   |  3. ПЛАВАЮЩИЙ ТРАНСПОРТ       |
|     (Entropy Shaping)         |     (Fountain / Raptor Codes) |     (Chameleon Core)          |
+-------------------------------+-------------------------------+-------------------------------+
| Обратное арифметическое       | Разрушение понятия единой     | Ядро отделено от протокола.   |
| кодирование проецирует шифр   | сессии. Пакеты нарезаются на  | Логика фрейминга и обфускации |
| в поток со структурированной  | осколки и передаются через    | мутирует по псевдослучайной   |
| энтропией (4.5–5.5 бит/байт), | разные динамические маршруты. | машине состояний, согласуемой |
| похожей на медиаданные.       | FEC восстанавливает данные.   | сервером и клиентом на лету.  |
+-------------------------------+-------------------------------+-------------------------------+
```

#### I. Арифметическое формирование распределения (Entropy & Distribution Shaping)
* Вместо маскировки заголовками применяется математическое преобразование: тело зашифрованного пакета пропускается через обратный арифметический кодер (Inverse Arithmetic Coder), ориентированный на статистическую модель легитимных бинарных потоков (сжатые текстуры, медиа-контейнеры).
* Статистический анализатор ТСПУ видит естественное распределение байт и структурированную энтропию без характерной "криптографической полки" $8.0$.

#### II. Дисперсия трафика и Фонтанные коды (Packet Scattering / Raptor Codes)
* Устранение классического «сессионного следа» (Flow Analysis): соединение не привязывается к одной паре IP:Port.
* Пакеты разбиваются на $K$ исходных + $M$ контрольных микро-дейтаграмм по алгоритму фонтанного кодирования (Fountain / Reed-Solomon FEC).
* Осколки отправляются через различные физические маршруты. На каждом промежуточном узле цензор видит несвязанный фоновый шум без возможности собрать контекст сессии.
* **Игровой режим (Zero-Latency):** Потеря до 15–20% дейтаграмм на сети не вызывает повторных запросов (Retransmission) и скачков задержки (Head-of-Line Blocking) — данные восстанавливаются на приёмнике мгновенно.

#### III. Архитектура сменных транспортов (Chameleon Core)
* **Разделение слоёв:**
  * **Неизменяемое системное ядро:** Драйвер Wintun, L3-маршрутизация Windows/Linux, lockless кольцевые буферы, Zero-Allocation пайплайны.
  * **Сменный динамический транспорт:** Правила фрейминга, сдвиги полей, длина джиттер-паддинга и транспортные носители не зашиваются статично в код клиента.
* **Синхронизация на лету:** Параметры мутации генерируются согласованной псевдослучайной машиной состояний (PRNG), инициализированной эфемерным ключом сессии ($X25519$ + $HKDF$).
* **Устойчивость к анализу кода:** Даже при наличии открытого исходного кода на GitHub трафик конкретной сессии невозможно классифицировать без знания закрытого ключа.

### 6.3. Стратегия работы в режиме «Белых списков» (Whitelisting Bypass)
1. **Паразитический транспорт (Substrate Tunneling):**
   * Инкапсуляция микро-фреймов в разрешённые протоколы и сети через легитимную Anycast/CDN-инфраструктуру, не подлежащую блокировке без нарушения работы критических сервисов.
2. **Stealth Handshake & Защита от Active Probing (Decoy Fallback):**
   * Первичный пакет клиента криптографически неотличим от шума или стандартного TLS ClientHello.
   * При поступлении запроса от стороннего сканера или сотрудника цензурного ведомства без валидной криптографической подписи, серверная нода функционирует как аутентичный веб-сайт (Decoy Reverse Proxy) с валидным доверенным SSL-сертификатом, не выдавая факт наличия туннельного демона.
3. **Dead-Drop Discovery (Скрытый роутинг):**
   * Динамическое обнаружение рабочих точек входа через белые каналы (зашифрованные DNS TXT доверенных публичных резолверов, децентрализованные метаданные).

### 6.4. Дополнительные векторы противодействия на уровне L4/L7
1. **TCP Desync & TTL Manipulation (Физический обман ТСПУ):**
   * ТСПУ установлен на оборудовании провайдера на расстоянии нескольких хопов от клиента (например, хоп 3 или 4), в то время как целевой сервер находится на хопе 12+.
   * Генерация опережающих пакетов с урезанным значением `TTL = 3`: пакет гарантированно считывается инспектором ТСПУ и сбивает его машину состояний (State Machine), но физически сгорает в сети до достижения реального сервера.
   * Следом отправляется валидный пакет с `TTL = 64`, который беспрепятственно доходит до сервера через ослепленный DPI.
2. **Port Hopping (Плавающие динамические порты UDP):**
   * Ротация портов каждые 30–60 секунд по псевдослучайному алгоритму (FHSS-подобный механизм).
   * ТСПУ не успевает накопить устойчивый статистический профиль (Flow Statistics) на связку `IP:Port`, воспринимая трафик как разрозненные короткие всплески.
3. **Fake ClientHello Splitting (Микросегментация потока):**
   * Разрыв первичного TLS ClientHello на микросегменты (1–2 байта в первом фрейме с контролируемой задержкой в 15–25 мс).
   * Потоковые буферы EcoFilter/DPI с ограниченной очередью не способны удерживать контекст разорванных сессий при терабитном трафике и пропускают сессию без инспекции.

### 6.5. Реализация на .NET 10 (Zero-Allocation Engine)
* **System.IO.Pipelines:** потоковая обработка кадров через `PipeReader` / `PipeWriter` без копирования между буферами сокета и ядра.
* **Span-first Архитектура:** парсинг заголовков через `ReadOnlySpan<byte>` и `ref struct` без создания объектов в управляемой куче (аллокация $0$ байт на горячем пути).
* **Аппаратная криптография:** прямое использование векторных расширений процессора (AES-NI, ARMv8 Crypto Extensions) для обеспечения пропускной способности $> 10$ Гбит/с на ядро.

---

## 7. Виртуальный полигон ТСПУ и валидация на реальной сети (Virtual TSPU & In-Situ Diagnostics)

### 7.1. Архитектура локального эмулятора `VirtualTspu`
Для непрерывного тестирования протокола прямо в среде разработки и CI/CD создаётся автономный аналитический модуль `VirtualTspu`, воспроизводящий логику работы промышленных комплексов EcoFilter:

```
+---------------------------------------------------------------------------------------------------+
|                                 LOKAL VIRTUAL TSPU TESTBED                                        |
+---------------------------------+---------------------------------+-------------------------------+
|  1. ЭНТРОПИЙНЫЙ ДЕТЕКТОР        |  2. ГИСТОГРАММА ДЛИН И IAT      |  3. СИГНАТУРЫ И АКТИВНЫЙ ЗОНД |
+---------------------------------+---------------------------------+-------------------------------+
| Скользящее окно 256 байт.       | Кластеризация размеров пакетов; | Поиск известных маркеров      |
| Вычисление энтропии Шеннона:    | анализ дельты времени (IAT).    | (0x01 Wireguard, OpenVPN).    |
|   H = - \sum p_i \log_2(p_i)    | Выявление искусственных пауз    | Отправка проверочных запросов |
| Полка H > 7.8 -> ДЕТЕКТИРОВАНО  | и непрерывных потоков.          | сканером на порт сервиса.     |
+---------------------------------+---------------------------------+-------------------------------+
```

* **Моделируемые вердикты:**
  * `DROP_ENTROPY_ANOMALY` — обнаружение монотонного криптографического шума.
  * `DROP_KNOWN_SIGNATURE` — совпадение длин и опкодов известных VPN-рукопожатий.
  * `INJECT_FAKE_TCP_RST` — отправка фейкового сброса TCP при обнаружении запрещённых SNI.
  * `THROTTLE_QUIC_UDP` — вероятностный сброс 80% дейтаграмм на порт 443.
  * `ACTIVE_PROBE_FAILED` — сервер раскрыл себя при внешнем зондировании.

### 7.2. Преодоление проблемы расхождения тестов («Pass в тестах, но Fail в реальности»)
Локальный юнит-тест работает в стерильных условиях. В реальной сети ТСПУ ведёт себя иначе из-за:
1. Асимметричной маршрутизации у провайдеров (исходящий и входящий трафик идут через разные узлы).
2. Различий в прошивках ТСПУ (разные поколения EcoFilter и балансировщиков Eco Highway у МТС, Ростелекома, Мегафона).
3. Динамического порога срабатывания ML-классификаторов по объёму накопленного трафика.

### 7.3. In-Situ сетевая диагностика: Проверка без развертывания серверов
Чтобы тестировать устойчивость протокола к реальному ТСПУ без аренды тяжелой серверной инфраструктуры, клиентская подсистема `Obxodka` использует метод **активной локальной телеметрии (In-Situ Probing)**:

1. **TTL-триангуляция и картирование ТСПУ (Hop Distance Mapping):**
   * Клиент отправляет серию SYN/UDP-запросов к заведомо фильтруемому ресурсу с пошаговым увеличением TTL ($1, 2, 3 \dots$).
   * По первому узлу, который присылает аномальный ответ (инжектированный TCP RST с нестандартным IP ID или закрытие без ICMP Time Exceeded), вычисляется **точная дистанция до ТСПУ в хопах**.
2. **Пассивный спектральный анализ RST-пакетов (RST Fingerprinting):**
   * Пакеты сброса, генерируемые ТСПУ, имеют характерные аномалии:
     * Значение TTL часто отлично от TTL серверов назначения (например, резкий скачок с 52 до 64 или 128).
     * Поле `IP Identification` (IP ID) часто обнулено или содержит специфический паттерн генератора EcoFilter.
     * Несоответствие номера подтверждения `ACK Number` текущему окну TCP.
   * Клиент мгновенно фиксирует вмешательство ТСПУ и автоматически переключает стратегию десинхронизации.
3. **Канареечное эхо-тестирование (Canary In-Band Echo):**
   * Использование общедоступных легитимных публичных резолверов (Cloudflare 1.1.1.1, Google 8.8.8.8) и белых Anycast-узлов для отправки замаскированных тестовых фреймов.
   * Сравнение статуса прохождения пакетов сквозь реальную магистраль без необходимости содержать выделенные тестовые серверы.
