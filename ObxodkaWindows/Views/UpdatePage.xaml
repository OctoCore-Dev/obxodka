<Page x:Class="ObxodkaWindows.UpdatePage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      Title="Обновление" Background="Transparent"
      d:DesignHeight="520" d:DesignWidth="380"
      mc:Ignorable="d">

    <Grid Margin="20,10,20,10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Image Grid.Row="0" Source="pack://application:,,,/Resources/appicon.png" 
               Width="60" Height="60" HorizontalAlignment="Center" Margin="0,10,0,10">
            <Image.Effect>
                <DropShadowEffect BlurRadius="10" Color="#FF0044" Opacity="0.3" ShadowDepth="0"/>
            </Image.Effect>
        </Image>

        <StackPanel Grid.Row="1" HorizontalAlignment="Center" Margin="0,0,0,15">
            <TextBlock Text="НОВАЯ ВЕРСИЯ" FontSize="24" FontWeight="Bold" Foreground="White" HorizontalAlignment="Center"/>
            <TextBlock x:Name="VersionLabel" Text="v1.0.0" FontSize="14" Foreground="#FF0044" FontWeight="Bold" HorizontalAlignment="Center"/>
        </StackPanel>

        <Border Grid.Row="2" Background="#111113" CornerRadius="20" BorderBrush="#2A2A2A" BorderThickness="1" Padding="15">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>
                <TextBlock Grid.Row="0" Text="ЧТО НОВОГО:" FontSize="10" Foreground="Gray" FontWeight="Bold" Margin="0,0,0,8"/>

                <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
                    <TextBlock x:Name="ChangelogText" 
                               Text="Загрузка списка изменений..." 
                               Foreground="#AAAAAA" FontSize="13" 
                               TextWrapping="Wrap" LineHeight="18"/>
                </ScrollViewer>
            </Grid>
        </Border>

        <StackPanel Grid.Row="3" Margin="0,15,0,10">
            <Border Height="50" CornerRadius="25">
                <Border.Background>
                    <LinearGradientBrush StartPoint="0,0" EndPoint="1,0">
                        <GradientStop Color="#FF0044" Offset="0.0" />
                        <GradientStop Color="#660011" Offset="1.0" />
                    </LinearGradientBrush>
                </Border.Background>
                <Button Content="ОБНОВИТЬ СЕЙЧАС" Click="OnDownloadUpdateClicked"
                        Background="Transparent" BorderThickness="0" Foreground="White" 
                        FontWeight="Bold" FontSize="16" Cursor="Hand">
                    <Button.Style>
                        <Style TargetType="Button">
                            <Setter Property="Template">
                                <Setter.Value>
                                    <ControlTemplate TargetType="Button">
                                        <Border x:Name="btnBorder" Background="Transparent" CornerRadius="25">
                                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                        </Border>
                                        <ControlTemplate.Triggers>
                                            <Trigger Property="IsMouseOver" Value="True">
                                                <Setter TargetName="btnBorder" Property="Background" Value="#22FFFFFF"/>
                                            </Trigger>
                                            <Trigger Property="IsPressed" Value="True">
                                                <Setter TargetName="btnBorder" Property="Background" Value="#44FFFFFF"/>
                                            </Trigger>
                                        </ControlTemplate.Triggers>
                                    </ControlTemplate>
                                </Setter.Value>
                            </Setter>
                        </Style>
                    </Button.Style>
                </Button>
            </Border>

            <Button x:Name="SkipButton" Click="OnSkipClicked" Margin="0,10,0,0" Cursor="Hand">
                <Button.Style>
                    <Style TargetType="Button">
                        <Setter Property="Template">
                            <Setter.Value>
                                <ControlTemplate TargetType="Button">
                                    <Border x:Name="border" 
                                            Background="Transparent" 
                                            BorderBrush="#33FFFFFF" 
                                            BorderThickness="1" 
                                            CornerRadius="20" 
                                            Padding="20,8">
                                        <TextBlock x:Name="txt" Text="МОЖЕТ ПОЗЖЕ" 
                                                   Foreground="#88FFFFFF" 
                                                   FontSize="11" 
                                                   FontWeight="Medium"
                                                   HorizontalAlignment="Center" 
                                                   VerticalAlignment="Center"/>
                                    </Border>
                                    <ControlTemplate.Triggers>
                                        <Trigger Property="IsMouseOver" Value="True">
                                            <Setter TargetName="border" Property="BorderBrush" Value="#88FFFFFF"/>
                                            <Setter TargetName="txt" Property="Foreground" Value="White"/>
                                        </Trigger>
                                        <Trigger Property="IsPressed" Value="True">
                                            <Setter Property="Opacity" Value="0.7"/>
                                        </Trigger>
                                    </ControlTemplate.Triggers>
                                </ControlTemplate>
                            </Setter.Value>
                        </Setter>
                    </Style>
                </Button.Style>
            </Button>
        </StackPanel>
    </Grid>
</Page>