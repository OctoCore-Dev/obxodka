<Page x:Class="ObxodkaWindows.PaymentPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:wv2="clr-namespace:Microsoft.Web.WebView2.Wpf;assembly=Microsoft.Web.WebView2.Wpf"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      Title="Оплата" Background="Transparent"
      d:DesignHeight="520" d:DesignWidth="380"
      mc:Ignorable="d">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="75"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="25"/>
        </Grid.RowDefinitions>

        <Grid Grid.Row="0" VerticalAlignment="Center" Margin="25,10,25,0">
            <TextBlock Text="ОПЛАТА ТОКЕНОВ" Foreground="White" 
                       FontWeight="Bold" FontSize="18" HorizontalAlignment="Left" Opacity="0.9"/>
        </Grid>

        <Grid Grid.Row="1" Margin="20,0,20,0">
            <Border x:Name="OuterFrame" CornerRadius="0" Background="#0F0F10" BorderBrush="#252528" BorderThickness="1"/>

            <Grid>
                <wv2:WebView2 x:Name="PayWebView" DefaultBackgroundColor="#0F0F10" Margin="0"/>

                <Grid x:Name="LoadingOverlay" Visibility="Visible" Background="#0F0F10">
                    <StackPanel VerticalAlignment="Center">
                        <Grid Width="45" Height="45" HorizontalAlignment="Center">
                            <Ellipse Stroke="#1A1A1C" StrokeThickness="3"/>
                            <Ellipse Stroke="#FF0044" StrokeThickness="3" StrokeDashArray="3,1.5">
                                <Ellipse.RenderTransform>
                                    <RotateTransform x:Name="SpinTransform" CenterX="22.5" CenterY="22.5"/>
                                </Ellipse.RenderTransform>
                                <Ellipse.Triggers>
                                    <EventTrigger RoutedEvent="Loaded">
                                        <BeginStoryboard>
                                            <Storyboard>
                                                <DoubleAnimation Storyboard.TargetProperty="(UIElement.RenderTransform).(RotateTransform.Angle)"
                                                                 From="0" To="360" Duration="0:0:1.2" RepeatBehavior="Forever"/>
                                            </Storyboard>
                                        </BeginStoryboard>
                                    </EventTrigger>
                                </Ellipse.Triggers>
                            </Ellipse>
                        </Grid>
                        <TextBlock Text="ЗАГРУЗКА..." Foreground="#FF0044" HorizontalAlignment="Center" 
                                   Margin="0,20,0,0" FontSize="11" FontWeight="Bold"/>
                    </StackPanel>
                </Grid>
            </Grid>

            <Border CornerRadius="0" BorderBrush="#252528" BorderThickness="1" IsHitTestVisible="False"/>
        </Grid>

        <Border Grid.Row="2" Background="Transparent" />
    </Grid>
</Page>