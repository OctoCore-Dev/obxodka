<?xml version="1.0" encoding="utf-8" ?>
<ContentView
    x:Class="obxodka.Views.MeshView"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:mi="http://www.aathifmahir.com/dotnet/2022/maui/icons">

    <Grid
        x:Name="RootLayout"
        Padding="{OnIdiom Desktop='32,28',
                          Phone='16,20,16,120',
                          Default='16,20'}"
        RowDefinitions="Auto, *"
        RowSpacing="16"
        SafeAreaEdges="None">

        <Grid Grid.Row="0" ColumnDefinitions="Auto, *">
            <Label
                Grid.Column="0"
                FontFamily="{DynamicResource AppFontTitle}"
                FontSize="{OnIdiom Desktop=22,
                                   Default=24}"
                Text="Mesh Сеть"
                VerticalOptions="Center" />

            <HorizontalStackLayout
                x:Name="MeshStatusBadgeContainer"
                Grid.Column="1"
                HorizontalOptions="End"
                Spacing="6"
                VerticalOptions="Center">
                <BoxView
                    x:Name="MeshStatusDot"
                    CornerRadius="5"
                    HeightRequest="10"
                    VerticalOptions="Center"
                    WidthRequest="10"
                    Color="{DynamicResource Error}" />
                <Label
                    x:Name="MeshStatusBadge"
                    FontFamily="{DynamicResource AppFontBold}"
                    FontSize="13"
                    LineBreakMode="TailTruncation"
                    Text="Релей неактивен"
                    TextColor="{DynamicResource Error}"
                    VerticalOptions="Center" />
            </HorizontalStackLayout>
        </Grid>

        <ScrollView
            x:Name="MeshScrollView"
            Grid.Row="1"
            HorizontalOptions="Fill"
            HorizontalScrollBarVisibility="Never"
            VerticalScrollBarVisibility="Never">
            <VerticalStackLayout
                x:Name="MeshCardsStack"
                HorizontalOptions="Start"
                Spacing="16">

                <Border
                    x:Name="CardRelayServer"
                    Padding="20"
                    Style="{StaticResource NeoCard}">
                    <VerticalStackLayout Spacing="14">
                        <Grid ColumnDefinitions="Auto, *, Auto">
                            <Border
                                Grid.Column="0"
                                BackgroundColor="{DynamicResource PrimaryDim}"
                                HeightRequest="48"
                                StrokeShape="RoundRectangle 24"
                                StrokeThickness="0"
                                VerticalOptions="Center"
                                WidthRequest="48">
                                <mi:MauiIcon
                                    HorizontalOptions="Center"
                                    Icon="{mi:Fluent Icon=Server24}"
                                    IconColor="{DynamicResource Primary}"
                                    IconSize="24"
                                    VerticalOptions="Center" />
                            </Border>
                            <VerticalStackLayout
                                Grid.Column="1"
                                Margin="12,0,0,0"
                                VerticalOptions="Center">
                                <Label
                                    FontFamily="{DynamicResource AppFontBold}"
                                    FontSize="16"
                                    Text="Релей-сервер (Раздача в фоне)" />
                                <Label
                                    FontSize="12"
                                    Style="{StaticResource LabelMuted}"
                                    Text="Раздавайте трафик в фоне и получайте бонусные часы" />
                            </VerticalStackLayout>
                            <Switch
                                x:Name="RelayServerSwitch"
                                Grid.Column="2"
                                Toggled="OnRelayServerToggledAsync"
                                VerticalOptions="Center" />
                        </Grid>

                        <Border
                            Padding="14,10"
                            BackgroundColor="{DynamicResource BgElevated}"
                            StrokeThickness="0">
                            <Border.StrokeShape>
                                <RoundRectangle CornerRadius="12" />
                            </Border.StrokeShape>
                            <Grid ColumnDefinitions="*, *, *" ColumnSpacing="8">
                                <VerticalStackLayout Grid.Column="0" Spacing="2">
                                    <Label
                                        FontSize="10"
                                        Style="{StaticResource LabelMuted}"
                                        Text="КЛИЕНТЫ" />
                                    <Label
                                        x:Name="ActiveClientsCountLabel"
                                        FontFamily="{DynamicResource AppFontBold}"
                                        FontSize="20"
                                        Text="0"
                                        TextColor="{DynamicResource Primary}" />
                                </VerticalStackLayout>
                                <VerticalStackLayout Grid.Column="1" Spacing="2">
                                    <Label
                                        FontSize="10"
                                        Style="{StaticResource LabelMuted}"
                                        Text="СКОРОСТЬ" />
                                    <Label
                                        x:Name="CurrentThroughputLabel"
                                        FontFamily="{DynamicResource AppFontBold}"
                                        FontSize="20"
                                        Text="0.0"
                                        TextColor="{DynamicResource Success}" />
                                    <Label
                                        FontSize="9"
                                        Style="{StaticResource LabelMuted}"
                                        Text="Мбит/с" />
                                </VerticalStackLayout>
                                <VerticalStackLayout Grid.Column="2" Spacing="2">
                                    <Label
                                        FontSize="10"
                                        Style="{StaticResource LabelMuted}"
                                        Text="ПЕРЕДАНО" />
                                    <Label
                                        x:Name="TotalRelayedMbLabel"
                                        FontFamily="{DynamicResource AppFontBold}"
                                        FontSize="20"
                                        Text="0.0"
                                        TextColor="{DynamicResource Primary}" />
                                    <Label
                                        FontSize="9"
                                        Style="{StaticResource LabelMuted}"
                                        Text="МБ" />
                                </VerticalStackLayout>
                            </Grid>
                        </Border>
                    </VerticalStackLayout>
                </Border>

                <Border
                    x:Name="CardReward"
                    Padding="20"
                    Style="{StaticResource NeoCard}">
                    <VerticalStackLayout Spacing="12">
                        <Grid ColumnDefinitions="Auto, *">
                            <Border
                                Grid.Column="0"
                                BackgroundColor="{DynamicResource PrimaryDim}"
                                HeightRequest="48"
                                StrokeShape="RoundRectangle 24"
                                StrokeThickness="0"
                                VerticalOptions="Center"
                                WidthRequest="48">
                                <mi:MauiIcon
                                    HorizontalOptions="Center"
                                    Icon="{mi:Fluent Icon=Gift24}"
                                    IconColor="{DynamicResource Primary}"
                                    IconSize="24"
                                    VerticalOptions="Center" />
                            </Border>
                            <VerticalStackLayout
                                Grid.Column="1"
                                Margin="12,0,0,0"
                                VerticalOptions="Center">
                                <Label
                                    FontFamily="{DynamicResource AppFontBold}"
                                    FontSize="16"
                                    Text="Награда за раздачу (+5 часов)" />
                                <Label
                                    FontSize="12"
                                    Style="{StaticResource LabelMuted}"
                                    Text="Каждые 5 ГБ розданного трафика = +5 часов к подписке" />
                            </VerticalStackLayout>
                        </Grid>

                        <VerticalStackLayout Spacing="4">
                            <Grid ColumnDefinitions="*, Auto">
                                <Label
                                    x:Name="RelayRewardProgressLabel"
                                    FontSize="12"
                                    Style="{StaticResource LabelMuted}"
                                    Text="0.0 / 5.0 ГБ (0%)" />
                                <Label
                                    x:Name="TotalLifetimeRelayedLabel"
                                    Grid.Column="1"
                                    FontSize="12"
                                    Style="{StaticResource LabelMuted}"
                                    Text="Всего: 0 ГБ" />
                            </Grid>
                            <ProgressBar
                                x:Name="RelayRewardProgressBar"
                                HeightRequest="8"
                                Progress="0.0"
                                ProgressColor="{DynamicResource Primary}" />
                        </VerticalStackLayout>

                        <Button
                            x:Name="ClaimRewardButton"
                            Clicked="OnClaimRewardClickedAsync"
                            HeightRequest="42"
                            IsEnabled="False"
                            Text="Забрать награду (+5 часов)" />
                    </VerticalStackLayout>
                </Border>

                <Border
                    x:Name="CardLimits"
                    Padding="20"
                    Style="{StaticResource NeoCard}">
                    <VerticalStackLayout Spacing="12">
                        <Label
                            FontFamily="{DynamicResource AppFontBold}"
                            FontSize="16"
                            Text="Ограничения ресурсов" />

                        <VerticalStackLayout Spacing="4">
                            <Grid ColumnDefinitions="*, Auto">
                                <Label
                                    FontSize="13"
                                    Style="{StaticResource LabelMuted}"
                                    Text="Максимальная скорость" />
                                <Label
                                    x:Name="SpeedLimitValueLabel"
                                    Grid.Column="1"
                                    FontFamily="{DynamicResource AppFontBold}"
                                    FontSize="13"
                                    Text="10 Мбит/с"
                                    TextColor="{DynamicResource Primary}" />
                            </Grid>
                            <Slider
                                x:Name="SpeedLimitSlider"
                                Maximum="100"
                                Minimum="1"
                                ValueChanged="OnSpeedLimitChanged"
                                Value="10" />
                        </VerticalStackLayout>

                        <Grid ColumnDefinitions="*, Auto">
                            <Label
                                FontSize="13"
                                Style="{StaticResource LabelMuted}"
                                Text="Макс. одновременных клиентов"
                                VerticalOptions="Center" />
                            <Picker
                                x:Name="MaxClientsPicker"
                                Grid.Column="1"
                                SelectedIndexChanged="OnMaxClientsChanged"
                                WidthRequest="90">
                                <Picker.Items>
                                    <x:String>1</x:String>
                                    <x:String>2</x:String>
                                    <x:String>3</x:String>
                                    <x:String>5</x:String>
                                    <x:String>10</x:String>
                                </Picker.Items>
                            </Picker>
                        </Grid>
                    </VerticalStackLayout>
                </Border>

                <Border
                    x:Name="CardMyCode"
                    Padding="20"
                    Style="{StaticResource NeoCard}">
                    <VerticalStackLayout Spacing="12">
                        <Grid ColumnDefinitions="Auto, *">
                            <Border
                                Grid.Column="0"
                                BackgroundColor="{DynamicResource PrimaryDim}"
                                HeightRequest="48"
                                StrokeShape="RoundRectangle 24"
                                StrokeThickness="0"
                                VerticalOptions="Center"
                                WidthRequest="48">
                                <mi:MauiIcon
                                    HorizontalOptions="Center"
                                    Icon="{mi:Fluent Icon=Share24}"
                                    IconColor="{DynamicResource Primary}"
                                    IconSize="24"
                                    VerticalOptions="Center" />
                            </Border>
                            <VerticalStackLayout
                                Grid.Column="1"
                                Margin="12,0,0,0"
                                VerticalOptions="Center">
                                <Label
                                    FontFamily="{DynamicResource AppFontBold}"
                                    FontSize="16"
                                    Text="Мой код приглашения (+1 час)" />
                                <Label
                                    FontSize="12"
                                    Style="{StaticResource LabelMuted}"
                                    Text="Друг получает +1 час, а вам — +1 час в подарок" />
                            </VerticalStackLayout>
                        </Grid>

                        <Border
                            Padding="16,12"
                            BackgroundColor="{DynamicResource PrimaryDim}"
                            Stroke="{DynamicResource BorderMedium}"
                            StrokeThickness="1">
                            <Border.StrokeShape>
                                <RoundRectangle CornerRadius="12" />
                            </Border.StrokeShape>
                            <Grid ColumnDefinitions="*, Auto, Auto" ColumnSpacing="10">
                                <Label
                                    x:Name="MyReferralCodeLabel"
                                    Grid.Column="0"
                                    CharacterSpacing="2"
                                    FontFamily="{DynamicResource AppFontBold}"
                                    FontSize="20"
                                    Text="OBX-......"
                                    TextColor="{DynamicResource Primary}"
                                    VerticalOptions="Center" />
                                <Button
                                    Grid.Column="1"
                                    Clicked="OnCopyMyCodeClickedAsync"
                                    HeightRequest="36"
                                    Text="Копировать" />
                                <Button
                                    Grid.Column="2"
                                    Clicked="OnShareMyCodeClickedAsync"
                                    HeightRequest="36"
                                    Style="{StaticResource BtnSecondary}"
                                    Text="Поделиться" />
                            </Grid>
                        </Border>
                    </VerticalStackLayout>
                </Border>

                <Border
                    x:Name="CardFriendCode"
                    Padding="20"
                    Style="{StaticResource NeoCard}">
                    <VerticalStackLayout Spacing="12">
                        <Grid ColumnDefinitions="Auto, *">
                            <Border
                                Grid.Column="0"
                                BackgroundColor="{DynamicResource PrimaryDim}"
                                HeightRequest="48"
                                StrokeShape="RoundRectangle 24"
                                StrokeThickness="0"
                                VerticalOptions="Center"
                                WidthRequest="48">
                                <mi:MauiIcon
                                    HorizontalOptions="Center"
                                    Icon="{mi:Fluent Icon=PersonAdd24}"
                                    IconColor="{DynamicResource Primary}"
                                    IconSize="24"
                                    VerticalOptions="Center" />
                            </Border>
                            <VerticalStackLayout
                                Grid.Column="1"
                                Margin="12,0,0,0"
                                VerticalOptions="Center">
                                <Label
                                    FontFamily="{DynamicResource AppFontBold}"
                                    FontSize="16"
                                    Text="Активировать код друга" />
                                <Label
                                    FontSize="12"
                                    Style="{StaticResource LabelMuted}"
                                    Text="Введите код — вам и другу начислят +1 час" />
                            </VerticalStackLayout>
                        </Grid>

                        <Grid ColumnDefinitions="*, Auto" ColumnSpacing="10">
                            <Border
                                Grid.Column="0"
                                Padding="12,0"
                                BackgroundColor="{DynamicResource BgInput}"
                                HeightRequest="44"
                                Stroke="{DynamicResource BorderSubtle}"
                                StrokeThickness="1">
                                <Border.StrokeShape>
                                    <RoundRectangle CornerRadius="10" />
                                </Border.StrokeShape>
                                <Entry
                                    x:Name="FriendCodeEntry"
                                    CharacterSpacing="1"
                                    FontSize="14"
                                    Placeholder="OBX-XXXXXX"
                                    VerticalOptions="Center" />
                            </Border>
                            <Button
                                x:Name="ActivateFriendCodeButton"
                                Grid.Column="1"
                                Clicked="OnActivateFriendCodeClickedAsync"
                                HeightRequest="44"
                                Text="Применить" />
                        </Grid>
                    </VerticalStackLayout>
                </Border>

                <Border
                    x:Name="CardFriendsList"
                    Padding="20"
                    Style="{StaticResource NeoCard}">
                    <VerticalStackLayout Spacing="12">
                        <Grid ColumnDefinitions="*, Auto">
                            <Label
                                FontFamily="{DynamicResource AppFontBold}"
                                FontSize="16"
                                Text="Приглашённые друзья"
                                VerticalOptions="Center" />
                            <Label
                                x:Name="FriendsCountLabel"
                                Grid.Column="1"
                                FontFamily="{DynamicResource AppFontBold}"
                                FontSize="13"
                                Text="0 / 10"
                                TextColor="{DynamicResource Primary}"
                                VerticalOptions="Center" />
                        </Grid>

                        <VerticalStackLayout x:Name="FriendsListContainer" Spacing="8">
                            <Label
                                x:Name="NoFriendsPlaceholder"
                                Margin="0,8"
                                FontSize="13"
                                HorizontalOptions="Center"
                                Style="{StaticResource LabelMuted}"
                                Text="Пока нет друзей. Поделитесь кодом!" />
                        </VerticalStackLayout>
                    </VerticalStackLayout>
                </Border>

                <Border
                    x:Name="CardSecurity"
                    Padding="14"
                    BackgroundColor="{DynamicResource SuccessDim}"
                    StrokeThickness="0">
                    <Border.StrokeShape>
                        <RoundRectangle CornerRadius="12" />
                    </Border.StrokeShape>
                    <Grid ColumnDefinitions="Auto, *" ColumnSpacing="12">
                        <mi:MauiIcon
                            Grid.Column="0"
                            Icon="{mi:Fluent Icon=ShieldCheckmark24}"
                            IconColor="{DynamicResource Success}"
                            IconSize="26"
                            VerticalOptions="Center" />
                        <Label
                            Grid.Column="1"
                            FontSize="12"
                            LineBreakMode="WordWrap"
                            Text="Сквозное шифрование: Релей пересылает только зашифрованные TLS 1.3 / TLS 1.2 байты. Личные данные и содержимое сайтов защищены ключами сервера."
                            TextColor="{DynamicResource Success}" />
                    </Grid>
                </Border>

            </VerticalStackLayout>
        </ScrollView>
    </Grid>
</ContentView>
