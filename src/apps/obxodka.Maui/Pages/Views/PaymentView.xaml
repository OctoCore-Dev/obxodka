<?xml version="1.0" encoding="utf-8" ?>
<ContentView
    x:Class="obxodka.Views.PaymentView"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:mi="http://www.aathifmahir.com/dotnet/2022/maui/icons">

    <Grid Padding="{OnIdiom Desktop='32,24', Phone='16,16,16,100'}" RowDefinitions="Auto, *">
        <VerticalStackLayout
            Grid.Row="0"
            Margin="0,0,0,20"
            HorizontalOptions="Center"
            Spacing="4">
            <Label
                FontFamily="{DynamicResource AppFontTitle}"
                FontSize="{OnIdiom Desktop=24,
                                   Default=22}"
                HorizontalOptions="Center"
                Text="ТАРИФНЫЕ ПЛАНЫ"
                TextColor="{DynamicResource TextPrimary}" />
            <Label
                FontSize="13"
                HorizontalOptions="Center"
                Style="{StaticResource LabelMuted}"
                Text="Выберите удобный пакет часов доступа без ограничений по скорости" />
        </VerticalStackLayout>

        <Grid Grid.Row="1" VerticalOptions="Center">
            <ScrollView
                x:Name="CardsScrollView"
                HorizontalScrollBarVisibility="Never"
                Orientation="Vertical"
                VerticalScrollBarVisibility="Never">
                <Grid
                    x:Name="CardsContainer"
                    ColumnDefinitions="{OnIdiom Desktop='*, *, *',
                                                Phone='*'}"
                    ColumnSpacing="{OnIdiom Desktop=16,
                                            Phone=0}"
                    HorizontalOptions="Center"
                    MaximumWidthRequest="{OnIdiom Desktop=920,
                                                  Default=380}"
                    RowSpacing="{OnIdiom Desktop=0,
                                         Phone=14}">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="{OnIdiom Desktop=0, Phone=Auto}" />
                        <RowDefinition Height="{OnIdiom Desktop=0, Phone=Auto}" />
                    </Grid.RowDefinitions>

                    <Border
                        x:Name="Card300"
                        Grid.Row="0"
                        Grid.Column="0"
                        Padding="22,20"
                        Style="{StaticResource NeoCard}">
                        <VerticalStackLayout Spacing="10">
                            <Label
                                FontFamily="{DynamicResource AppFontBold}"
                                FontSize="11"
                                Text="СТАРТОВЫЙ"
                                TextColor="{DynamicResource Primary}" />
                            <Label
                                FontFamily="{DynamicResource AppFontTitle}"
                                FontSize="28"
                                Text="300 ₽"
                                TextColor="{DynamicResource Primary}" />
                            <Label
                                FontFamily="{DynamicResource AppFontMedium}"
                                FontSize="14"
                                Text="300 часов доступа" />
                            <Label
                                FontSize="12"
                                LineBreakMode="WordWrap"
                                Style="{StaticResource LabelMuted}"
                                Text="Отличный выбор для знакомства и редких поездок." />

                            <Border
                                x:Name="Btn300Border"
                                Margin="0,8,0,0"
                                HeightRequest="44"
                                Style="{StaticResource NeoBtnPrimary}">
                                <Grid>
                                    <Button
                                        x:Name="Btn300"
                                        BackgroundColor="Transparent"
                                        Clicked="OnBuy300ClickedAsync"
                                        FontFamily="{DynamicResource AppFontMedium}"
                                        FontSize="13"
                                        Text="Купить за 300 ₽"
                                        TextColor="{DynamicResource TextPrimary}" />
                                    <ActivityIndicator
                                        x:Name="Loader300"
                                        HeightRequest="22"
                                        HorizontalOptions="Center"
                                        IsRunning="True"
                                        IsVisible="False"
                                        VerticalOptions="Center"
                                        WidthRequest="22"
                                        Color="{DynamicResource TextPrimary}" />
                                </Grid>
                            </Border>
                        </VerticalStackLayout>
                    </Border>

                    <Border
                        x:Name="Card500"
                        Grid.Row="{OnIdiom Desktop=0,
                                           Phone=1}"
                        Grid.Column="{OnIdiom Desktop=1,
                                              Phone=0}"
                        Padding="22,20"
                        Stroke="{DynamicResource Accent}"
                        StrokeThickness="1.5"
                        Style="{StaticResource NeoCard}">
                        <VerticalStackLayout Spacing="10">
                            <Grid ColumnDefinitions="*, Auto">
                                <Label
                                    Grid.Column="0"
                                    FontFamily="{DynamicResource AppFontBold}"
                                    FontSize="11"
                                    Text="ОПТИМАЛЬНЫЙ"
                                    TextColor="{DynamicResource Accent}" />
                                <Border
                                    Grid.Column="1"
                                    Padding="6,2"
                                    BackgroundColor="{DynamicResource BorderSubtle}"
                                    Stroke="{DynamicResource Accent}"
                                    StrokeShape="RoundRectangle 4"
                                    StrokeThickness="1">
                                    <Label
                                        FontFamily="{DynamicResource AppFontBold}"
                                        FontSize="10"
                                        Text="ХИТ"
                                        TextColor="{DynamicResource Accent}" />
                                </Border>
                            </Grid>

                            <Label
                                FontFamily="{DynamicResource AppFontTitle}"
                                FontSize="28"
                                Text="500 ₽"
                                TextColor="{DynamicResource Accent}" />
                            <Label
                                FontFamily="{DynamicResource AppFontMedium}"
                                FontSize="14"
                                Text="700 часов (+200ч бонус)" />
                            <Label
                                FontSize="12"
                                LineBreakMode="WordWrap"
                                Style="{StaticResource LabelMuted}"
                                Text="Идеально на месяц активного стриминга и игр." />

                            <Border
                                x:Name="Btn500Border"
                                Margin="0,8,0,0"
                                BackgroundColor="{DynamicResource Accent}"
                                HeightRequest="44"
                                Stroke="{DynamicResource Accent}"
                                Style="{StaticResource NeoBtnPrimary}">
                                <Grid>
                                    <Button
                                        x:Name="Btn500"
                                        BackgroundColor="Transparent"
                                        Clicked="OnBuy500ClickedAsync"
                                        FontFamily="{DynamicResource AppFontBold}"
                                        FontSize="13"
                                        Text="Купить за 500 ₽"
                                        TextColor="{DynamicResource BgBase}" />
                                    <ActivityIndicator
                                        x:Name="Loader500"
                                        HeightRequest="22"
                                        HorizontalOptions="Center"
                                        IsRunning="True"
                                        IsVisible="False"
                                        VerticalOptions="Center"
                                        WidthRequest="22"
                                        Color="{DynamicResource BgBase}" />
                                </Grid>
                            </Border>
                        </VerticalStackLayout>
                    </Border>

                    <Border
                        x:Name="Card1000"
                        Grid.Row="{OnIdiom Desktop=0,
                                           Phone=2}"
                        Grid.Column="{OnIdiom Desktop=2,
                                              Phone=0}"
                        Padding="22,20"
                        Style="{StaticResource NeoCard}">
                        <VerticalStackLayout Spacing="10">
                            <Label
                                FontFamily="{DynamicResource AppFontBold}"
                                FontSize="11"
                                Text="МАКСИМУМ (ВЫГОДНЫЙ)"
                                TextColor="{DynamicResource Warning}" />
                            <Label
                                FontFamily="{DynamicResource AppFontTitle}"
                                FontSize="28"
                                Text="1000 ₽"
                                TextColor="{DynamicResource Warning}" />
                            <Label
                                FontFamily="{DynamicResource AppFontMedium}"
                                FontSize="14"
                                Text="2000 часов (х2 бонус)" />
                            <Label
                                FontSize="12"
                                LineBreakMode="WordWrap"
                                Style="{StaticResource LabelMuted}"
                                Text="Максимальная выгода для нескольких устройств." />

                            <Border
                                x:Name="Btn1000Border"
                                Margin="0,8,0,0"
                                BackgroundColor="{DynamicResource Warning}"
                                HeightRequest="44"
                                Stroke="{DynamicResource Warning}"
                                Style="{StaticResource NeoBtnPrimary}">
                                <Grid>
                                    <Button
                                        x:Name="Btn1000"
                                        BackgroundColor="Transparent"
                                        Clicked="OnBuy1000ClickedAsync"
                                        FontFamily="{DynamicResource AppFontBold}"
                                        FontSize="13"
                                        Text="Купить за 1000 ₽"
                                        TextColor="{DynamicResource BgBase}" />
                                    <ActivityIndicator
                                        x:Name="Loader1000"
                                        HeightRequest="22"
                                        HorizontalOptions="Center"
                                        IsRunning="True"
                                        IsVisible="False"
                                        VerticalOptions="Center"
                                        WidthRequest="22"
                                        Color="{DynamicResource BgBase}" />
                                </Grid>
                            </Border>
                        </VerticalStackLayout>
                    </Border>

                </Grid>
            </ScrollView>
        </Grid>
    </Grid>
</ContentView>
